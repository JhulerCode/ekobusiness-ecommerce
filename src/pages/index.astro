---
import BaseLayout from '../layouts/BaseLayout.astro';
import CategoriasDestacadas from '../components/CategoriasDestacadas.astro';
import ProductosDestacados from '../components/ProductosDestacados.astro';
import { getCategoriasDestacadas, getProductosDestacados } from '../lib/api';

const categorias = await getCategoriasDestacadas();
const productos = await getProductosDestacados();
---

<BaseLayout title="Inicio - Sunka">
    <!-- Banner / Slider -->
    <section
        class="relative w-full h-[80vh] flex items-center justify-center overflow-hidden"
    >
        <!-- Imagen de fondo -->
        <img
            src="https://economia3.com/wp-content/uploads/2021/02/oportunidad-de-negocio-en-el-mundo-del-te.jpg"
            alt="Tienda de tés naturales"
            class="absolute inset-0 w-full h-full object-cover filter brightness-50 contrast-100 saturate-0"
        />

        <!-- Overlay degradado -->
        <!-- <div class="absolute inset-0 bg-black/40"></div> -->

        <!-- Contenido centrado -->
        <div class="relative z-10 text-center max-w-2xl px-6">
            <h1 class="text-4xl md:text-6xl font-heading text-white mb-4">
                Vive la experiencia del <span class="text-secondary"
                    >té natural</span
                >
            </h1>
            <p class="text-lg md:text-xl text-neutral-light mb-6">
                Descubre sabores únicos que cuidan tu cuerpo y tu mente.
            </p>
            <a
                href="/productos"
                class="inline-block px-8 py-4 bg-primary text-white font-semibold rounded-xl shadow-xl hover:bg-primary/90 transition relative border-2 border-white/30"
            >
                <span class="relative z-10">Ver productos</span>
                <!-- Glow detrás del botón -->
                <span
                    class="absolute inset-0 rounded-xl bg-primary/50 blur-xl opacity-70"
                ></span>
            </a>
        </div>
    </section>

    <!-- Categorías destacadas -->
    <CategoriasDestacadas categorias={categorias} />

    <!-- Productos populares -->
    <ProductosDestacados productos={productos} />

    
    <section>
        <h2>Más vendidos</h2>
        <div class="grid">
            <article>🍵 Té Verde Matcha</article>
            <article>🌸 Infusión Frutos Rojos</article>
            <article>🎁 Pack Degustación</article>
        </div>
    </section>

    <!-- Beneficios -->
    <section class="benefits">
        <div>🚚 Envíos gratis desde $50</div>
        <div>🍃 Ingredientes naturales</div>
        <div>🔒 Pago 100% seguro</div>
    </section>

    <!-- Newsletter -->
    <section class="newsletter">
        <h2>Suscríbete y recibe 10% OFF</h2>
        <form>
            <input type="email" placeholder="Tu correo" required />
            <button type="submit">Suscribirme</button>
        </form>
    </section>
</BaseLayout>
